# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

주 사용자는 두 단계로 이어진다.

1. **채용 담당자 / 리크루터 — 1차 스크리닝.** 이력서나 `portfolio.pdf`와 함께 링크를 받아, 몇 분 안에 다음 단계로 넘길지 판단한다. 빠른 스캔과 증거 제시가 최우선이다.
2. **실무 면접관 (엔지니어·리드) — 기술 검증.** 면접 전후로 판단 근거, 검증 방법, 실패 보고까지 읽는다. 깊이와 검증 과정이 최우선이다.

표면(Home/Projects 목록)은 스크리닝용, 상세 페이지(`/projects/[slug]`)는 검증용이라는 2단 구조가 사용자 가정의 핵심이다.

## Product Purpose

편채범(AI Engineer)의 개인 웹 포트폴리오. LLM Agent / AX 직무 지원 시 제출용 PDF와 동일한 사실을 웹에서 더 깊이 검증할 수 있게 만든다. 성공은 리크루터가 몇 분 안에 "다음 단계"를 결정하고, 실무 면접관이 상세 페이지만 읽고도 판단 근거와 검증 방식을 재구성할 수 있는 상태다.

## Positioning

"PoC를 데모에서 끝내지 않는다" — 현업 기준으로 범위를 정하고, 실제 케이스로 재고, 안 된 이유까지 분류해 문서로 남기는 방식 자체가 포지셔닝이다. 이웃한 포트폴리오가 그대로 복사할 수 없는 것은 실측 수치(34건 평균 21.5분), 정답 라벨을 직접 만든 검증셋, 그리고 **실패를 숨기지 않고 원인별로 분류해 공개하는 항목**(로컬 LLM 실패 4분류, self-training 성능 저하 원인 규명)이다. 실패 보고는 장식이 아니라 포지셔닝의 일부다. 다만 그 자리는 상세 페이지(`/projects/[slug]`의 결과·실패 절)이며, Home의 계기 3개는 실측 성과 지표로 쓴다. 상세에서 축소하거나 감추면 제품 가치가 훼손된다.

## Operating Context

- 링크는 보통 지원서·이력서·PDF와 함께 전달된다. 사이트 단독 유입은 예외적이다.
- `public/portfolio.pdf`는 제출용 PDF이며 사이트와 같은 사실을 담는다. 웹과 PDF의 수치·문구가 어긋나면 신뢰가 깨진다.
- 열람은 데스크톱과 모바일 모두에서 일어난다(메일에서 바로 여는 경로 포함).
- 지원사별로 강조색을 교체해 대응할 예정이라, 사이트는 지원 시점마다 조금씩 다른 색으로 제출된다.

## Capabilities and Constraints

- **스택:** Next.js 16 (App Router) · React 19 · TypeScript · Tailwind CSS v4. `output: "export"` 정적 export만 사용하며 서버 기능·런타임 API·이미지 최적화가 없다.
- **배포:** GitHub Pages 사용자 사이트(`https://dev-pyun.github.io`). `basePath`/`assetPrefix` 없음. `main` push 시 `.github/workflows/deploy.yml`이 lint / type-check / build 후 배포한다.
- **내용의 단일 출처:** `content/projects.ts`. 모든 문구·수치는 여기서만 수정한다. 페이지가 문자열을 직접 들고 있으면 PDF와의 동기화가 깨진다.
- **라우트:** Home(`/`), Projects(`/projects`, `/projects/[slug]`), About, Contact, not-found.
- **언어:** 한국어(`<html lang="ko">`). `word-break: keep-all`로 한국어 줄바꿈을 제어하고 있다. 영문 병기·다국어는 미정.
- **테마 교체 제약(확정):** 강조색은 CSS 변수 토큰 **1개** 교체로 전체가 따라오는 구조를 유지한다. 색은 장식이 아니라 교체 가능한 슬롯이며, 인쇄된 색 값이 컴포넌트에 흩어지면 이 제약이 깨진다. 사이트 내 테마 토글 UI는 만들지 않는다(리크루터에게 불필요한 노출).
- **공개 범위 제약:** 손해사정 프로젝트는 가명처리된 의료·보험 데이터를 다루는 비공개 연구다. 코드·프롬프트·케이스는 공개하지 않고, 문제 구조·시스템 흐름·본인의 판단과 검증 방식으로 한정해 기술한다. 개인 연락처는 이메일만 표시한다.
- **용어:** CFMask, mIoU, self-training, pseudo label, 해기차, 손해사정 등 도메인 용어를 그대로 쓴다. 두 독자 모두 전문 용어를 감당하므로 하향 번역하지 않는다.

## Brand Commitments

- 이름 **편채범**, 직무 표기 **AI Engineer**, 태그라인 "현업 업무를 끝까지 돌아가는 AI 파이프라인으로 만드는 사람".
- 소속: 서울대학교 지구환경과학부 · 인공지능 복수전공.
- 본문 서체 **Pretendard Variable** (CDN 로드, 한국어 본문 품질 때문에 선택됨).
- 목소리: 단정하고 사실 중심. 과장·마케팅 수사 없이 수치와 근거로 말하고, 안 된 것은 안 됐다고 쓴다.
- **현재 오렌지(`#F37321` / 본문용 `#C8520E`)는 브랜드 커밋먼트가 아니다.** 한화 계열 지원을 염두에 둔 초기 값이며 지원사별 교체 대상이다. 향후 시각 작업은 이 색을 보존 의무로 취급하지 않는다.

## Evidence on Hand

- `content/projects.ts` — 프로젝트 5건의 확정 문구·표·수치 단일 출처. 실측값(손해사정 34건 평균 21.5분, CFMask 대비 Recall +3.68%p / F1 +1.83%p, 해기차 상관 0.16/0.04와 일별 −0.6~0.8, 5팀 중 1위, 수업 A+ 2건) 포함.
- `public/images/` — 발표자료에서 추출한 결과 이미지 16장(구름 마스크 비교 6장, 산점도·히스토그램 2장, 위성영상 1장, 극한강설 종관 분석 7장: 300hPa V-wind Hovmöller 3일치·300hPa 제트·700hPa 해기차·기상청 500hPa 일기도 2장). 일기도 2장은 기상청 발행 자료이며 캡션에 출처를 명시한다.
- `public/portfolio.pdf` — 제출용 PDF.
- 공개 저장소 2건: `dev-pyun/CMA-project`, `dev-pyun/Wiki_Racing-dev-pyun`.
- **추가 확보 가능(확인됨):** 발표 자료에 아직 사이트에 들어오지 않은 공개 가능 이미지가 더 있고, 10단계 에이전트 하니스·U-Net self-training 단계 등의 **아키텍처 다이어그램 / 파이프라인 도해를 새로 만들 수 있다**. 상세 페이지 강화는 이 재료를 전제로 설계해도 된다.
- **없는 것(지어내면 안 됨):** 고객사·사용 기업 사례, 외부 인용이나 추천사, 재직 이력, 수상 내역(교내 성적우수상 2회 외), 트래픽·사용자 수, 오픈소스 기여 통계. 손해사정 프로젝트의 코드·프롬프트·실제 케이스·원본 서류 이미지는 존재하더라도 공개 불가다.

## Product Principles

1. **두 독자를 한 화면에 겹쳐 태우지 않는다.** 표면은 몇 분 안의 판단을, 상세는 재구성 가능한 검증을 각각 책임진다. 한쪽을 위해 다른 쪽을 희생하는 타협안을 만들지 않는다.
2. **수치와 출처가 주장을 대신한다.** 형용사로 설명해야 하는 성과는 아직 성과가 아니다. 모든 숫자는 조건(무엇 대비, 몇 건, 어떤 검증셋)과 함께 나타난다.
3. **실패는 1급 콘텐츠다.** 안 된 실험과 그 원인 분류는 상세 페이지의 눈에 띄는 자리에 놓인다. 접어두거나 톤을 낮추는 것은 이 포트폴리오의 핵심 차별점을 깎는 일이다.
4. **비공개 경계는 설계 조건이다.** 못 보여주는 프로젝트도 문제 구조·흐름·판단으로 설득력을 갖도록 만든다. 공개 불가를 이유로 빈약하게 남겨두지 않는다.
5. **사실은 한 곳에만 산다.** `content/projects.ts`와 PDF가 어긋나지 않게 유지하는 것이 시각적 야심보다 앞선다.
6. **색은 교체 가능한 슬롯이다.** 아이덴티티는 타이포·레이아웃·구조에서 나오고, 강조색 하나를 바꿔도 무너지지 않는다.

## Accessibility & Inclusion

- 현재 구현: 스킵 링크("본문으로 건너뛰기"), `:focus-visible` 아웃라인, `prefers-reduced-motion` 대응, 모든 결과 이미지에 한국어 `alt`. 이 수준은 회귀시키지 않는다.
- 한국어 가독성은 접근성 요건으로 취급한다(`keep-all` 줄바꿈, 본문 line-height 1.65).
- 준수해야 할 외부 표준은 확정된 바 없다. 명시적 요구가 생기기 전까지 위 항목이 기준선이다.
